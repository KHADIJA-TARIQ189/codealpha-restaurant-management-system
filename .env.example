PORT=5000
JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=8h
NODE_ENV=development
# Set to "true" only in local dev if you want Sequelize to try to auto-reconcile schema
# changes on startup. Leave "false"/unset elsewhere — see the comment in src/server.js.
DB_SYNC_ALTER=false
